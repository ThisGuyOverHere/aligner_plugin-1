const keyMirror = require('keymirror');
let ProjectConstants = keyMirror({
    SET_JOB_ID: null,
    RENDER_ROWS: null,
    GET_ROWS: null,
    CHANGE_SEGMENT_POSITION: null
});

export default ProjectConstants;